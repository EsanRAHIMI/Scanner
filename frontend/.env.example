# --- Domain (optional; default: ehsanrahimi.com) ---
# NEXT_PUBLIC_APP_BASE_DOMAIN=YOUR_DOMAIN

# --- URL overrides (optional; auto-built from domain when unset in production) ---
# BACKEND_DETECT_URL=https://lorenzo.YOUR_DOMAIN/api/detect
# TRAINER_API_BASE=https://trainer.YOUR_DOMAIN/api

# Local
BACKEND_DETECT_URL=http://127.0.0.1:8000/detect
TRAINER_API_BASE=http://127.0.0.1:8010
