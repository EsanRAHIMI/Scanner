# Server-side target for /api/image proxy (Dokploy: internal service URL or public API origin)
IMAGE_API_BASE=http://localhost:8020

# Optional legacy override (do not append /api — paths already use /api/v1/...)
# NEXT_PUBLIC_IMAGE_API_BASE=http://localhost:8020

# Trainer auth proxy (/api/trainer/*) — production: https://trainer.YOUR_DOMAIN/api
NEXT_PUBLIC_TRAINER_API_BASE=http://localhost:8010
