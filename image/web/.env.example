# Production (image.lorenzohome.ae + /api on same host): browser calls /api/v1 directly.
# Server-side fallback (SSR / web-api proxy is optional):
IMAGE_API_BASE=https://image.lorenzohome.ae

# Trainer auth via /web-api/trainer/* on the web host
NEXT_PUBLIC_TRAINER_API_BASE=https://trainer.lorenzohome.ae/api

# Local dev
# IMAGE_API_BASE=http://localhost:8020
# NEXT_PUBLIC_TRAINER_API_BASE=http://localhost:8010
NEXT_PUBLIC_AGENT_URL=http://localhost:8040