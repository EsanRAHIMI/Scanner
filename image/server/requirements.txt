fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
Pillow>=10.4.0
rembg[cpu]>=2.0.76
boto3==1.35.76
google-api-python-client==2.154.0
google-auth==2.37.0
pydantic==2.10.3
pydantic-settings==2.6.1
aiofiles==24.1.0
pymongo==4.10.1

# ---- Optional, for higher-quality / standardized output (safe to omit) ----
# premium quality matting + foreground decontamination (CPU). If absent, the
# self-hosted premium path falls back to a dependency-free guided-filter method.
# pymatting==1.1.12
# AVIF web renditions (IMAGE_RENDER_WEB_AVIF=true). If absent, AVIF is skipped.
# pillow-avif-plugin==1.4.6
